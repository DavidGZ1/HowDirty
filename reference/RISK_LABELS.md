# Risk level labels

Named character vector mapping integer risk levels (0-6) to
human-readable labels. Use this constant when displaying or comparing
risk levels to ensure consistency.

## Usage

``` r
RISK_LABELS
```

## Format

An object of class `character` of length 7.
