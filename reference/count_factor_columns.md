# Short description

description

## Usage

``` r
count_factor_columns(df)
```

## Arguments

- df:

  data frame whose factor columns are counted.

## Value

data frame with one row per factor column and a Count column.

## Examples

``` r
if (FALSE) { # \dontrun{
count_factor_columns(my_df)
} # }
```
